import { District, DistrictDto } from "@/resources/dto/studio";

import { managementApiClient } from "./apiClient";

export const createUser = async () => {
  //   const { items } = await managementApiClient
};
